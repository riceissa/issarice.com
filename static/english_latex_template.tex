\RequirePackage[l2tabu,orthodox]{nag}

%% Add `,titlepage` after `letterpaper` to get a separate page
%% for the title information.
\documentclass[12pt,letterpaper]{article}%

%%%%%%%%%%%%%%%%%%%% Packages
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{datetime}

%% Use 1 inch margins (all around).
\usepackage[margin=1in]{geometry}%

%% For double spacing, etc. See `\doublespacing` and
%% `\onehalfspacing` below.
%\usepackage{setspace}%

%% New paragraphs get an empty line rather than an indent.
%\usepackage[parfill]{parskip}%

%% Use `\ul{text}` to underline text.
%\usepackage{soul}%

\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{amsfonts}
\usepackage{amssymb}
%\usepackage{mhchem}

%% Example: `\mathscr{A}`.
\usepackage{mathrsfs}%

%% Enable this and `thmtools` to get theorem-like environments.
%\usepackage{amsthm}%

%% Note: `qed=...` only works with TeXLive2011 or newer (I think).
%% For older distributions, just remove `qed=...`.  Also, theorem
%% declarations must come before `hyperref`.
%\usepackage{thmtools}

%\declaretheorem[style=definition,qed=\(\lrcorner\),parent=subsection]%
    %{axiom}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{corollary}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{definition}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{discussion}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{example}

%% Here are two styles for exercises.
%\declaretheoremstyle[headfont=\itshape,notefont=\itshape,%
    %notebraces={}{},numbered=no]{myexercise}
%\declaretheorem[style=myexercise]%
    %{exercise}

%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{exercise}

%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{lemma}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{note}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{proposition}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{remark}
%\declaretheorem[style=definition,qed=\(\lrcorner\),sibling=axiom]%
    %{theorem}

%% For units.  Use for example `\SI{12.345}{\gram}`.
%\usepackage{siunitx}%

\usepackage[all,error]{onlyamsmath}
\usepackage{fixltx2e}
\usepackage{booktabs}
%\usepackage{multirow}
\usepackage{array}
\usepackage{paralist}
\usepackage{verbatim}
\usepackage{subfig}
\usepackage{graphicx}

%% Use for example `\begin{figure}[H] ... \end{figure}` to place a
%% figure exactly where it appears in the source.
\usepackage{float}

%% Use for example `\begin{itemize}[noitemsep] ... \end{itemize}`
%% to disable the space between items.
%% Also useful for resuming a particular counter:
%%     \begin{enumerate}[resume]
%%         \item Text.
%%     \end{enumerate}
%% will reuse the last counter.  See
%% http://tex.stackexchange.com/questions/1669/resuming-a-list/1670#1670
%% for more.
\usepackage{enumitem}

%% To obtain straight single (') and double (") quotes (instead of
%% curly ones); use `\textquotesingle` and `\textquotedbl`.  Note
%% however that `\textquotedbl` comes from the `fontenc` package.
%\usepackage{textcomp}%

%%%%%%%%%%%%%%%%%%%% Displaying source code
%% For displaying source code.  Use
%% `\lstinline[language=tex]!code here!`
%% to display (TeX) code within a paragraph.  Use
%% `\begin{lstlisting}[language=tex]code here\end{lstlisting}` to
%% show (TeX) code as its own paragraph.  Use
%% `\lstinputlisting[language=tex]{file.tex}` to display (TeX) code
%% from a separate file (`file.tex`).
%\usepackage{listings}%
%\lstset%
%{%
    %basicstyle=\ttfamily,%
    %keywordstyle=\bfseries,%
    %commentstyle=\slshape,%
    %stringstyle=\ttfamily,%
    %showspaces=false,%
    %showstringspaces=false,%
    %upquote=true,%
        %% Don't make quotes curly.
    %numbers=left%
%}%

%% For the International Phonetic Alphabet. Example:
%% `\textipa{text}`.
%\usepackage[tone]{tipa}%

%\usepackage{tikz}
%\usetikzlibrary{decorations.markings}
%\usetikzlibrary{snakes}

%%%%%%%%%%%%%%%%%%%% Fonts
%% Some fonts with (at least partial) maths support.

%% Computer Modern, but apparently enhanced.
\usepackage{lmodern}%

%% Times New Roman.
%\usepackage{mathptmx}%

%\usepackage{fouriernc}
%\usepackage{mathpazo}
%\usepackage{bookman}
%\usepackage{pxfonts}
%\usepackage[utopia]{mathdesign}
%\usepackage[charter]{mathdesign}

%%%%%%%%%%%%%%%%%%%% Packages (continued)
\usepackage{microtype}

%% Must be last package, except for `ellipsis` and `cleveref`.
%\usepackage{hyperref}%

%% When compiling with `platex`.
%\usepackage[dvipdfmx,setpagesize=false]{hyperref}

%% Must be after `hyperref`.
\usepackage{cleveref}%

%% Must be after `hyperref` (and probably `cleveref`).
\usepackage{ellipsis}

%%%%%%%%%%%%%%%%%%%% Document information
%% Remember to do a `\maketitle` later so that this information is
%% actually displayed in the resulting PDF.
%\title{TitleOfDocument}
%\author{AuthorOfDocument}

%% Uncomment and leave this empty to have no date.
%\date{\today{}}%

%%%%%%%%%%%%%%%%%%%% Custom commands
%% Example: "A \definiendum{group} is a ...".
%% Also see the Wikipedia articles "Separation of concerns" and
%% "Separation of presentation and content".
\newcommand{\definiendum}[1]{\textsl{#1}}%

%% To enable line breaks within a table cell.  See
%% http://tex.stackexchange.com/a/19678/18026 for more information.
\newcommand{\scell}[2][c]{%
    \begin{tabular}[#1]{@{}c@{}}#2\end{tabular}}%

%% This is the header I used for assignments in high school.
\newcommand{\schoolheader}[4]{\noindent #1\hfill #2

\hfill #3

\hfill #4

\bigskip}

%%%%% BEGIN define noindentnext
%% Do not indent the next paragraph; works even if there are blank
%% lines in between this command and the paragraph.
\makeatletter
\newcommand{\noindentnext}{\@afterindentfalse\@afterheading}
\makeatother
%%%%% END define noindentnext

\renewcommand{\dateseparator}{~}
\newcommand{\heading}[2]{\section*{#1}

\noindent\yyyymmdddate#2

\bigskip

\noindentnext}
\newcommand{\header}[2]{\heading{#1}{#2}}

%% This is to emulate the three dots or stars that are often used in
%% books.
\newcommand{\threestars}{\nobreak\begin{center}
\(\star~~~~\star~~~~\star\)
\end{center}

\nobreak\noindentnext}

\newcommand{\shift}{\nobreak\bigskip\noindentnext}
\newcommand{\dialogue}[1]{---\(\,\)#1}

%%%%%%%%%%%%%%%%%%%% For maths
%\crefname{axiom}{axiom}{axioms}
%\crefname{corollary}{corollary}{corollaries}
%\crefname{definition}{definition}{definitions}
%\crefname{discussion}{discussion}{discussions}
%\crefname{example}{example}{examples}
%\crefname{exercise}{exercise}{exercises}
%\crefname{lemma}{lemma}{lemmas}
%\crefname{note}{note}{notes}
%\crefname{proposition}{proposition}{propositions}
%\crefname{remark}{remark}{remarks}
%\crefname{theorem}{theorem}{theorems}

%% Abbreviations for sets of numbers.
%\newcommand{\N}{\ensuremath{\mathbf{N}}}
%\newcommand{\Z}{\ensuremath{\mathbf{Z}}}
%\newcommand{\Q}{\ensuremath{\mathbf{Q}}}
%\newcommand{\R}{\ensuremath{\mathbf{R}}}
%\newcommand{\C}{\ensuremath{\mathbf{C}}}

%% For absolute values and norms.  See
%% http://tex.stackexchange.com/a/43009 for the explanation.  Note that
%% this requires the mathtools package.
%\DeclarePairedDelimiter\abs{\lvert}{\rvert}%
%\DeclarePairedDelimiter\norm{\lVert}{\rVert}%

%% Swap the definition of \abs* and \norm*, so that \abs
%% and \norm resize the size of the brackets, and the
%% starred versions do not.
%\makeatletter
%\let\oldabs\abs
%\def\abs{\@ifstar{\oldabs}{\oldabs*}}
%
%\let\oldnorm\norm
%\def\norm{\@ifstar{\oldnorm}{\oldnorm*}}
%\makeatother

%%%%%%%%%%%%%%%%%%%% Other options
%% Use the following two with the `setspace` package (but not at the
%% same time).
%\doublespacing%
%\onehalfspacing

%%%%%%%%%%%%%%%%%%%% Begin document
\begin{document}
%\maketitle
%\tableofcontents
%\newpage
\frenchspacing

%\input{}

\end{document}
%%%%%%%%%%%%%%%%%%%% End document
